{% extends 'baseAdmin.html.twig' %}

{% block body %}
    <div class="container show-container">
        <h1>Bebida</h1>

        <table id="showSectionTable" class="display dataTable" cellspacing="0" width="100%">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ drink.id }}</td>
            </tr>
            <tr>
                <th>Código</th>
                <td>{{ drink.code }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ drink.name }}</td>
            </tr>
            <tr>
                <th>Descripción</th>
                <td>{{ drink.description }}</td>
            </tr>
            <tr>
                <th>Precio Unitario</th>
                <td>{{ drink.unitaryPrice }}</td>
            </tr>
            <tr>
                <th>Creado</th>
                <td>{% if drink.createdAt %}{{ drink.createdAt|date('Y-m-d H:i:s') }}{% endif %}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{% if drink.isActive %}Si{% else %}No{% endif %}</td>
            </tr>
            <tr>
                <th>Platos</th>
                <td>
                    <ul>
                        {% for item in drink.items %}
                            <li>{{ item|getItemById|title }}</li>
                        {% endfor %}
                    </ul>
                </td>
            </tr>
        </tbody>
    </table>

        <div class="row-fluid btn-container clearfix">
            <a href="{{ path('drink_edit', { 'id': drink.id }) }}" class="btn btn-primary">Editar</a>
            {{ form_start(delete_form) }}
                <input type="submit" value="Eliminar" class="btn btn-primary">
            {{ form_end(delete_form) }}
        </div>
    </div>
{% endblock %}
